# ai/ (Phase 6 — not yet implemented)

Will contain the Ollama client wrapper and prompt templates that take
content flagged by `detectors.engine.needs_ai_review()` and get a local
LLM's structured judgment (rationale, category, confidence) on ambiguous
content the deterministic layer can't resolve alone.

Not built in this guide (Phases 0-5). See the project's Phase 6 spec for scope.
