# dashboard/ (Phase 10 — not yet implemented)

Will contain the analyst-facing dashboard. Technology choice (server-rendered
Flask+HTML vs. a small React app) is deliberately deferred -- deciding it now
would be introducing an unjustified dependency ahead of need, which the
project's own environment-setup rule warns against.

Not built in this guide (Phases 0-5).
